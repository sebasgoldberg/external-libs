import Control from "sap/m/Button";

export default Control.extend("cencosud.libs.external.polyfill.control.MyControl",{
    metadata : {
    },
    init : function () {
    },
    renderer : {},
});
